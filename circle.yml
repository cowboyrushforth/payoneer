test:
  override:
    - echo "no tests"

deployment:
  production:
    branch: master
    commands:
      - gem install geminabox
      - gem build payoneer.gemspec
      - gem inabox payoneer-*.gem --host $GEMINABOX_URL
